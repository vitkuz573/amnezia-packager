version: '0.1.{build}'

image: Ubuntu2204

install:
  - sudo apt-get update -qq
  - sudo apt-get install -y -qq jq curl dpkg-dev fakeroot git

build_script:
  - echo "Hello"
  - mkdir -p output
  - echo "World" > output/test.txt

after_build:
  - ls -la output/

artifacts:
  - path: output/test.txt
    name: test

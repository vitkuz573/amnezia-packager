version: '0.1.{build}'

image: Ubuntu2204

install:
  - sudo apt-get update -qq
  - sudo apt-get install -y -qq jq curl

build_script:
  - echo "Hello from CI"
  - mkdir -p output
  - echo "test" > output/marker.txt
  - ls -la output/

artifacts:
  - path: output/marker.txt
    name: marker

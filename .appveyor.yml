version: '0.1.{build}'

image: Ubuntu2204

branches:
  only:
    - main

skip_commits:
  files:
    - '*.md'
    - docs/**
    - .github/**

environment:
  OUTPUT_DIR: /tmp/packages
  TEST_VAR: hello

build_script:
  - echo "Build running"
  - echo "Test var is $TEST_VAR"

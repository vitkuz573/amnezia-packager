pkgname = ${PKGNAME}
pkgver = ${PKGVER}-1
pkgdesc = ${PACKAGE_DESCRIPTION}
url = ${PACKAGE_URL}
builddate = $(date +%s)
packager = ${PACKAGE_VENDOR} Packager <${PACKAGE_URL}>
size = ${PKGSIZE_BYTES}
arch = any
license = ${PACKAGE_LICENSE}
${DEPS_ARCH_LINES}
